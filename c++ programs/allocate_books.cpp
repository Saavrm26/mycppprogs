#include <bits/stdc++.h>
using namespace std;
int books(vector<int> &A,int B){
    size_t l=A.size();
    int low=0,high=l+1;
    vector<int> a(l);
    copy(A.begin(),A.end(),a.begin());
    while(high-low>1){
        int mid=(high+low)/2;
        
    }    
}